#pragma once

#include "images-raw.hpp"
#include "image.hpp"

static const Image glassesImage = Image(image_glasses, 128, 64, 0, 0);
static const Image smallButtonImage = Image(image_small_button, 64, 64, 0, 0);
static const Image largeButtonImage = Image(image_large_button, 64, 64, 0, 0);