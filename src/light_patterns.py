# Existing code...

def alternate_day_pattern():
    # Define the alternate light pattern for bounties during the day
    pattern = [
        (255, 0, 0),  # Red
        (0, 255, 0),  # Green
        (0, 0, 255),  # Blue
    ]
    return pattern

# Existing code...